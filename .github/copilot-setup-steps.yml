steps:
  - name: Install Linux build dependencies
    run: |
      sudo apt-get update -q
      sudo apt-get install -y \
        cmake \
        mesa-common-dev \
        libx11-dev \
        libxi-dev \
        libxcursor-dev \
        libasound2-dev
